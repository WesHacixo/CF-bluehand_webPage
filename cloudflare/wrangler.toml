name = "bluehand-solutions"
compatibility_date = "2024-12-30"

# Cloudflare Pages configuration
# Note: For Pages deployments, build commands and environment variables
# are configured in the Cloudflare dashboard, not in wrangler.toml.
# This file is primarily for project identification.
pages_build_output_dir = "./"

# Custom domain configuration can be set in Cloudflare dashboard
# Build settings (build command, output directory) are configured in dashboard
