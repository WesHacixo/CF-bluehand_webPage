name = "bluehand-solutions"
compatibility_date = "2024-12-30"

# Pages configuration
pages_build_output_dir = "./"

# Note: [site] and [build] sections are not supported for Pages deployment
# Custom domain can be configured in Cloudflare dashboard
# Environment variables can be set in Cloudflare dashboard under Pages settings
