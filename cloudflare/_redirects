# Cloudflare Pages _redirects file
# URL redirects and rewrites

# Redirect www to non-www (or vice versa)
https://www.bluehand.solutions/* https://bluehand.solutions/:splat 301!

# Redirect HTTP to HTTPS (Cloudflare handles this automatically, but explicit is good)
http://bluehand.solutions/* https://bluehand.solutions/:splat 301!

# SPA fallback (if needed for future expansion)
# Note: Cloudflare Pages automatically serves 404.html for 404 errors
# /*  /index.html  200
